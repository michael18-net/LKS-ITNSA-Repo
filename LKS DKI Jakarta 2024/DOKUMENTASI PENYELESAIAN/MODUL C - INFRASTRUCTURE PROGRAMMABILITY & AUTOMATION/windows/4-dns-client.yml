---

- name: Playbook 4 Windows
  hosts: Windows

  tasks:
    - name: Set DNS nameservers on windows
      ansible.windows.win_dns_client:
        adapter_names: '*'
        dns_servers:
          - 10.0.10.20
          - 10.0.10.11